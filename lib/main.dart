import 'package:flutter/material.dart';
import 'package:quiz_app/quiz.dart';
void main() {
  //const - atored in memeory once and can be reused by dart 
  //constructor is a function/method in dart which has certain parameters and is called only when parameters is put it creates instances of a classs
  runApp(const Quiz());
}
